diff --git a/src/apps/discourse/tasks.py b/src/apps/discourse/tasks.py
index ef75dd8..7999d84 100644
--- a/src/apps/discourse/tasks.py
+++ b/src/apps/discourse/tasks.py
@@ -1,9 +1,10 @@
+from urllib.parse import parse_qs, urlparse
+
+import requests
 from django.conf import settings
 from django.contrib.auth.signals import user_logged_in, user_logged_out
 from django.dispatch import receiver
 from pydiscourse.sso import sso_validate, sso_redirect_url
-import requests
-import re
 
 
 @receiver(user_logged_in)
@@ -11,23 +12,28 @@ def discourse_login(sender, user, request, **kwargs):
     upstream = settings.DISCOURSE_UPSTREAM
     upstream = upstream[:-1] if upstream[-1] == '/' else upstream
 
-    response = requests.get(upstream + '/session/sso', allow_redirects=False)
+    session = requests.Session()
+    response = session.get(upstream + '/session/sso', allow_redirects=False)
     if response.status_code == 302:
         location = response.headers['Location']
 
-        regex = re.compile(r'sso=(.+)&sig=(.+)')
-        matches = regex.search(location)
-        payload, signature = matches.groups()
+        query = parse_qs(urlparse(location).query)
+        payload = query.get('sso', [None])[0]
+        signature = query.get('sig', [None])[0]
+        if not payload or not signature:
+            return
+
         secret = settings.DISCOURSE_SSO_SECRET
 
         nonce = sso_validate(payload, signature, secret)
         url = sso_redirect_url(nonce, secret, user.email, user.id,
                                user.username,
-                               name=user.get_full_name().encode('utf-8'))
-        response = requests.get(upstream + url, allow_redirects=False)
-        t_cookie = response.cookies.get('_t')
+                               name=user.get_full_name())
+        response = session.get(upstream + url, allow_redirects=False)
+        t_cookie = response.cookies.get('_t') or session.cookies.get('_t')
 
-        request.COOKIES['_t'] = t_cookie
+        if t_cookie:
+            request.set_cookies['_t'] = t_cookie
 
 
 @receiver(user_logged_out)
