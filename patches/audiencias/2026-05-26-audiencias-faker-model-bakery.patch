diff -ruN a/apps/accounts/backends.py b/apps/accounts/backends.py
--- a/apps/accounts/backends.py	2026-05-15 16:14:50.000000000 -0300
+++ b/apps/accounts/backends.py	2026-05-26 09:04:04.227239027 -0300
@@ -11,10 +11,12 @@
     def authenticate(self, request, remote_user):
         if not remote_user:
             return
-        user = None
-        remote_user_data = json.loads(
-            request.META.get('HTTP_REMOTE_USER_DATA')
-        )
+        remote_user_payload = request.META.get('HTTP_REMOTE_USER_DATA')
+        if not remote_user_payload:
+            return
+        remote_user_data = json.loads(remote_user_payload)
+        if 'email' not in remote_user_data:
+            return
         user, _ = UserModel.objects.get_or_create(
             email=remote_user_data['email']
         )
diff -ruN a/apps/accounts/middlewares.py b/apps/accounts/middlewares.py
--- a/apps/accounts/middlewares.py	2026-05-15 16:14:50.000000000 -0300
+++ b/apps/accounts/middlewares.py	2026-05-26 09:04:08.214254833 -0300
@@ -31,6 +31,8 @@
             if self.force_logout_if_no_header and request.user.is_authenticated:
                 self._remove_invalid_user(request)
             return
+        if not request.META.get('HTTP_REMOTE_USER_DATA'):
+            return
         # If the user is already authenticated and that user is the user we are
         # getting passed in the headers, then the correct user is already
         # persisted in the session and we don't need to continue.
diff -ruN a/apps/accounts/tests/test_accounts.py b/apps/accounts/tests/test_accounts.py
--- a/apps/accounts/tests/test_accounts.py	2026-05-15 16:14:49.000000000 -0300
+++ b/apps/accounts/tests/test_accounts.py	2026-05-26 08:48:34.559062822 -0300
@@ -1,5 +1,5 @@
 import pytest
-from mixer.backend.django import mixer
+from model_bakery import baker
 from apps.accounts.models import User, UserProfile
 
 
@@ -11,13 +11,13 @@
 
     @pytest.mark.django_db
     def test_create_user(self):
-        user = mixer.blend(
+        user = baker.make(
             User, is_active=True, first_name='test', last_name='user')
         assert User.objects.count() == 1
         assert user.__str__() == 'test user'
 
     @pytest.mark.django_db
     def test_create_profile(self):
-        user = mixer.blend(User, is_active=True)
+        user = baker.make(User, is_active=True)
         UserProfile.objects.create(user=user, is_admin=True)
         assert UserProfile.objects.count() == 1
diff -ruN a/apps/accounts/tests/test_remote_user.py b/apps/accounts/tests/test_remote_user.py
--- a/apps/accounts/tests/test_remote_user.py	2026-05-15 16:14:49.000000000 -0300
+++ b/apps/accounts/tests/test_remote_user.py	2026-05-26 09:02:30.551864950 -0300
@@ -1,5 +1,5 @@
 import pytest
-from mixer.backend.django import mixer
+from model_bakery import baker
 from apps.accounts.models import User, UserProfile
 from django.contrib.auth.models import AnonymousUser
 from django.test.client import RequestFactory
@@ -15,6 +15,10 @@
 )
 
 
+def get_response(request):
+    return None
+
+
 @pytest.fixture
 def test_user(db, django_user_model):
     return django_user_model.objects.create_user(
@@ -48,7 +52,7 @@
 def test_user_no_remote_user_middleware(test_user):
     request = RequestFactory().get('/')
     request.user = test_user
-    auth_middleware = AudienciasRemoteUser()
+    auth_middleware = AudienciasRemoteUser(get_response)
     response = auth_middleware.process_request(request)
 
     assert response == None
@@ -61,7 +65,7 @@
     request.user = test_user
     request.META['HTTP_AUTH_USER'] = 'invaliduser'
     request.session = client.session
-    auth_middleware = AudienciasRemoteUser()
+    auth_middleware = AudienciasRemoteUser(get_response)
     response = auth_middleware.process_request(request)
 
     assert response == None
@@ -71,7 +75,7 @@
 def test_utils_get_room_value_error():
     with pytest.raises(ImproperlyConfigured):
         request = RequestFactory().get('/')
-        auth_middleware = AudienciasRemoteUser()
+        auth_middleware = AudienciasRemoteUser(get_response)
         auth_middleware.process_request(request)
 
 
@@ -82,7 +86,7 @@
     request.user = test_user
     request.META['HTTP_AUTH_USER'] = 'testuser'
     request.session = client.session
-    auth_middleware = AudienciasRemoteUser()
+    auth_middleware = AudienciasRemoteUser(get_response)
     response = auth_middleware.process_request(request)
 
     assert response == None
@@ -94,7 +98,7 @@
     request = RequestFactory().get('/')
     request.user = test_user
     request.session = client.session
-    auth_middleware = AudienciasRemoteUser()
+    auth_middleware = AudienciasRemoteUser(get_response)
     auth_middleware.force_logout_if_no_header = True
     response = auth_middleware.process_request(request)
 
@@ -109,7 +113,7 @@
         request.user = test_user
         request.session = client.session
         del request.session[auth.BACKEND_SESSION_KEY]
-        auth_middleware = AudienciasRemoteUser()
+        auth_middleware = AudienciasRemoteUser(get_response)
         auth_middleware.force_logout_if_no_header = True
         auth_middleware.process_request(request)
 
@@ -123,7 +127,7 @@
     request.user = test_user
     request.session = client.session
     request.session[auth.BACKEND_SESSION_KEY] = backend_path
-    auth_middleware = AudienciasRemoteUser()
+    auth_middleware = AudienciasRemoteUser(get_response)
     auth_middleware.force_logout_if_no_header = True
     response = auth_middleware.process_request(request)
 
@@ -135,21 +139,21 @@
     from django.contrib.sessions.middleware import SessionMiddleware
     settings.AUTHENTICATION_BACKENDS = TEST_AUTHENTICATION_BACKENDS
     backend_path = 'apps.accounts.backends.AudienciasAuthBackend'
-    mixer.blend(User, username='testuser', email='test@e.com', is_active=True)
+    baker.make(User, username='testuser', email='test@e.com', is_active=True)
     user_data = {
         'email': 'test@e.com',
         'avatar': '/media/img/avatar.png',
         'first_name': 'test',
         'last_name': 'user'}
     request = RequestFactory().get('/')
-    middleware = SessionMiddleware()
+    middleware = SessionMiddleware(get_response)
     middleware.process_request(request)
     request.session.save()
     request.session[auth.BACKEND_SESSION_KEY] = backend_path
     request.user = AnonymousUser()
     request.META['HTTP_AUTH_USER'] = 'testuser'
     request.META['HTTP_REMOTE_USER_DATA'] = json.dumps(user_data)
-    auth_middleware = AudienciasRemoteUser()
+    auth_middleware = AudienciasRemoteUser(get_response)
     auth_middleware.process_request(request)
     user = User.objects.get(username='testuser')
 
@@ -163,24 +167,24 @@
     from django.contrib.sessions.middleware import SessionMiddleware
     settings.AUTHENTICATION_BACKENDS = TEST_AUTHENTICATION_BACKENDS
     backend_path = 'apps.accounts.backends.AudienciasAuthBackend'
-    mixer.blend(UserProfile, user=test_user)
+    baker.make(UserProfile, user=test_user)
     user_data = {
         'email': 'test@e.com',
         'avatar': '/media/img/avatar.png',
         'first_name': 'test',
         'last_name': 'user'}
     request = RequestFactory().get('/')
-    middleware = SessionMiddleware()
+    middleware = SessionMiddleware(get_response)
     middleware.process_request(request)
     request.session.save()
     request.session[auth.BACKEND_SESSION_KEY] = backend_path
     request.user = AnonymousUser()
     request.META['HTTP_AUTH_USER'] = 'testuser'
     request.META['HTTP_REMOTE_USER_DATA'] = json.dumps(user_data)
-    auth_middleware = AudienciasRemoteUser()
+    auth_middleware = AudienciasRemoteUser(get_response)
     auth_middleware.process_request(request)
     user = User.objects.get(username='testuser')
 
     assert user.first_name == 'test'
     assert user.last_name == 'user'
-    assert user.profile.avatar_url == '/media/img/avatar.png'
\ No newline at end of file
+    assert user.profile.avatar_url == '/media/img/avatar.png'
diff -ruN a/apps/core/consumers/room.py b/apps/core/consumers/room.py
--- a/apps/core/consumers/room.py	2026-05-15 16:14:50.000000000 -0300
+++ b/apps/core/consumers/room.py	2026-05-26 09:04:11.768268914 -0300
@@ -79,7 +79,7 @@
 
 
 def clean_text(text):
-    blackList = [x.strip() for x in config.WORDS_BLACK_LIST.split(',')]
+    blackList = [x.strip() for x in str(config.WORDS_BLACK_LIST).split(',')]
     wordList = re.sub(
         r"[^\w]", " ", text.lower()).split()
     censured_words = list(set(blackList) & set(wordList))
diff -ruN a/apps/core/tests/test_api.py b/apps/core/tests/test_api.py
--- a/apps/core/tests/test_api.py	2026-05-15 16:14:49.000000000 -0300
+++ b/apps/core/tests/test_api.py	2026-05-26 08:49:11.115259578 -0300
@@ -1,5 +1,5 @@
 import pytest
-from mixer.backend.django import mixer
+from model_bakery import baker
 from apps.core.models import Room
 from apps.core.permissions import ApiKeyPermission
 from apps.accounts.models import User
@@ -17,7 +17,7 @@
 
 @pytest.mark.django_db
 def test_rooms_api(client):
-    mixer.cycle(5).blend(Room)
+    baker.make(Room, _quantity=5)
     url = reverse('room-list')
     response = client.get(url, format='json')
 
@@ -28,7 +28,7 @@
 
 @pytest.mark.django_db
 def test_users_api(client):
-    mixer.blend(User, username='testuser', email='test@e.com', is_active=True)
+    baker.make(User, username='testuser', email='test@e.com', is_active=True)
     url = reverse('user-list')
     response = client.get(url, format='json')
 
diff -ruN a/apps/core/tests/test_commands.py b/apps/core/tests/test_commands.py
--- a/apps/core/tests/test_commands.py	2026-05-15 16:14:49.000000000 -0300
+++ b/apps/core/tests/test_commands.py	2026-05-26 08:48:34.560062827 -0300
@@ -1,7 +1,7 @@
 import pytest
 import responses
 from responses import matchers
-from mixer.backend.django import mixer
+from model_bakery import baker
 from django.conf import settings
 from apps.core.models import Room
 from apps.core.tests.mock_infoleg_api import RESPONSE_API
@@ -35,7 +35,7 @@
         json=RESPONSE_API, 
     )
     
-    mixer.blend(Room, cod_reunion=63610, youtube_status=1)
+    baker.make(Room, cod_reunion=63610, youtube_status=1)
     
     call_command("get_camara_webservice")
 
diff -ruN a/apps/core/tests/test_consumers.py b/apps/core/tests/test_consumers.py
--- a/apps/core/tests/test_consumers.py	2026-05-15 16:14:49.000000000 -0300
+++ b/apps/core/tests/test_consumers.py	2026-05-26 09:02:34.538880984 -0300
@@ -10,7 +10,7 @@
 from apps.core.utils import encrypt
 from apps.core.models import Room, Question
 from apps.accounts.models import User
-from mixer.backend.django import mixer
+from model_bakery import baker
 from asgiref.sync import sync_to_async
 import os
 
@@ -25,7 +25,7 @@
 
 @pytest.mark.django_db
 def test_utils_get_room():
-    room1 = mixer.blend(Room, is_active=True, pk=1)
+    room1 = baker.make(Room, is_active=True, pk=1)
     room2 = get_room(1)
     assert room1 == room2
 
@@ -56,6 +56,7 @@
 
 
 @pytest.mark.asyncio
+@pytest.mark.django_db
 async def test_home_consumer(settings):
     
     settings.CHANNEL_LAYERS = TEST_CHANNEL_LAYERS
@@ -90,7 +91,7 @@
     settings.CHANNEL_LAYERS = TEST_CHANNEL_LAYERS
     
     room = await sync_to_async(
-        mixer.blend)(Room, is_active=True, is_visible=True)
+        baker.make)(Room, is_active=True, is_visible=True, youtube_status=1)
 
     application = URLRouter([
         path('sala/<int:room_id>/perguntas/stream/',
@@ -126,9 +127,9 @@
 @pytest.mark.django_db
 async def test_room_consumer():
     room = await sync_to_async(
-        mixer.blend)(Room, is_active=True, is_visible=True)
+        baker.make)(Room, is_active=True, is_visible=True, youtube_status=1)
     user = await sync_to_async(
-        mixer.blend)(User, is_active=True)
+        baker.make)(User, is_active=True)
 
     application = URLRouter([
         path('sala/<int:room_id>/stream/',
@@ -145,7 +146,7 @@
 
     handler = encrypt(str(user.id).rjust(10))
     question = await sync_to_async(
-        mixer.blend)(Question, room=room)
+        baker.make)(Question, room=room)
 
     # Test send vote
     new_vote_data = {
diff -ruN a/apps/core/tests/test_models.py b/apps/core/tests/test_models.py
--- a/apps/core/tests/test_models.py	2026-05-15 16:14:49.000000000 -0300
+++ b/apps/core/tests/test_models.py	2026-05-26 09:01:14.067555181 -0300
@@ -1,9 +1,10 @@
 import pytest
-from mixer.backend.django import mixer
+from model_bakery import baker
 from apps.core.models import *
 from datetime import datetime, timedelta
 from django.urls import reverse
 from django.utils import timezone
+from django.utils.translation import gettext as _
 from constance import config as constance_config
 
 
@@ -15,77 +16,77 @@
 
 @pytest.mark.django_db
 def test_room_with_title():
-    room = mixer.blend(Room, title_reunion='title test')
+    room = baker.make(Room, title_reunion='title test')
     assert room.__str__() == 'title test'
 
 
 @pytest.mark.django_db
 def test_room_without_title():
-    room = mixer.blend(Room, title_reunion='')
-    assert room.__str__() == 'room'
+    room = baker.make(Room, title_reunion='')
+    assert room.__str__() == _('room')
 
 
 @pytest.mark.django_db
 def test_room_is_today():
-    room = mixer.blend(Room, date=datetime.now())
+    room = baker.make(Room, date=datetime.now())
     assert room.is_today() == True
     assert room.is_tomorrow() == False
 
 
 @pytest.mark.django_db
 def test_room_is_tomorrow():
-    room = mixer.blend(Room, date=datetime.now() + timedelta(days=1))
+    room = baker.make(Room, date=datetime.now() + timedelta(days=1))
     assert room.is_today() == False
     assert room.is_tomorrow() == True
 
 
 @pytest.mark.django_db
 def test_room_time_to_close():
-    room = mixer.blend(Room, closed_time=timezone.now(), youtube_status=2)
+    room = baker.make(Room, closed_time=timezone.now(), youtube_status=2)
     assert room.time_to_close() < 900 # 15 minutes
 
 
 @pytest.mark.django_db
 def test_room_without_time_to_close():
-    room = mixer.blend(Room, closed_time=None)
+    room = baker.make(Room, closed_time=None)
     assert room.time_to_close() == False
 
 
 @pytest.mark.django_db
 def test_room_main_video():
-    room = mixer.blend(Room)
-    video = mixer.blend(Video, room=room, is_attachment=False)
+    room = baker.make(Room)
+    video = baker.make(Video, room=room, is_attachment=False)
     assert room.latest_video() == video
     assert room.get_main_videos().count() == 1
 
 
 @pytest.mark.django_db
 def test_room_attachment_video():
-    room = mixer.blend(Room)
-    mixer.blend(Video, room=room, is_attachment=True)
+    room = baker.make(Room)
+    baker.make(Video, room=room, is_attachment=True)
     assert room.get_attachment_videos().count() == 1
 
 
 @pytest.mark.django_db
 def test_room_without_video():
     with pytest.raises(Video.DoesNotExist):
-        room = mixer.blend(Room)
+        room = baker.make(Room)
         room.latest_video()
 
 
 @pytest.mark.django_db
 def test_room_get_absolute_url():
-    room = mixer.blend(Room)
+    room = baker.make(Room)
     assert room.get_absolute_url() == reverse(
         'video_room', kwargs={'pk': room.id})
 
 
 @pytest.mark.django_db
 def test_room_properties(test_user):
-    room = mixer.blend(Room)
-    question = mixer.blend(Question, user=test_user, room=room)
-    mixer.blend(Message, user=test_user, room=room)
-    mixer.blend(UpDownVote, user=test_user, question=question)
+    room = baker.make(Room)
+    question = baker.make(Question, user=test_user, room=room)
+    baker.make(Message, user=test_user, room=room)
+    baker.make(UpDownVote, user=test_user, question=question)
     assert room.questions_count == 1
     assert room.messages_count == 1
     assert room.votes_count == 1
@@ -94,32 +95,38 @@
 
 @pytest.mark.django_db
 def test_vote_create(test_user):
-    vote = mixer.blend(UpDownVote, user=test_user)
+    vote = baker.make(
+        UpDownVote,
+        user=test_user,
+        question__room=baker.make(Room))
     assert vote.__str__() == 'testuser'
 
 
 @pytest.mark.django_db
 def test_message_create():
-    message = mixer.blend(Message, message='test message')
+    message = baker.make(Message, message='test message')
     assert message.__str__() == 'test message'
 
 
 @pytest.mark.django_db
 def test_video_create():
-    video = mixer.blend(Video, video_id='testId')
+    video = baker.make(Video, video_id='testId')
     assert video.__str__() == 'testId'
 
 
 @pytest.mark.django_db
 def test_question_create():
-    question = mixer.blend(Question, question='test question?')
+    question = baker.make(
+        Question,
+        question='test question?',
+        _fill_optional=True)
     assert question.__str__() == 'test question?'
 
 
 @pytest.mark.django_db
 def test_room_attachment_create():
-    room = mixer.blend(Room, title_reunion='title test')
-    attachment = mixer.blend(RoomAttachment, room=room)
+    room = baker.make(Room, title_reunion='title test')
+    attachment = baker.make(RoomAttachment, room=room)
     assert attachment.__str__() == 'title test'
 
 
@@ -135,7 +142,7 @@
     for key, value in constance_keys.items():
         setattr(constance_config, key, value)
 
-    room = mixer.blend(Room, youtube_status=2)
+    room = baker.make(Room, youtube_status=2)
     message = Message.objects.filter(room=room, user=test_user).first()
     video = Video.objects.filter(room=room).first()
 
@@ -147,4 +154,3 @@
     video.delete()
 
     assert room.videos.count() == 0
-
diff -ruN a/apps/core/tests/test_template_tags.py b/apps/core/tests/test_template_tags.py
--- a/apps/core/tests/test_template_tags.py	2026-05-15 16:14:49.000000000 -0300
+++ b/apps/core/tests/test_template_tags.py	2026-05-26 09:04:18.110294026 -0300
@@ -1,7 +1,7 @@
 import pytest
 from apps.core.templatetags.video_utils import *
 from apps.core.models import Question, Room, UpDownVote
-from mixer.backend.django import mixer
+from model_bakery import baker
 from django.contrib.auth.models import Group
 from apps.accounts.models import UserProfile
 
@@ -14,7 +14,11 @@
 
 @pytest.mark.django_db
 def test_vote_action_answered(test_user):
-    question = mixer.blend(Question, answered=True, user=test_user)
+    question = baker.make(
+        Question,
+        answered=True,
+        user=test_user,
+        _fill_optional=True)
     html = vote_action(question, test_user.username)
 
     assert 'Pergunta Respondida' in html
@@ -22,7 +26,11 @@
 
 @pytest.mark.django_db
 def test_vote_action_same_user(test_user):
-    question = mixer.blend(Question, answered=False, user=test_user)
+    question = baker.make(
+        Question,
+        answered=False,
+        user=test_user,
+        _fill_optional=True)
     html = vote_action(question, test_user.username)
 
     assert 'Sua Pergunta' in html
@@ -30,9 +38,9 @@
 
 @pytest.mark.django_db
 def test_vote_action_disabled_same_user(test_user):
-    room = mixer.blend(Room, youtube_status=2)
-    question = mixer.blend(Question, answered=False, room=room)
-    mixer.blend(UpDownVote, question=question, user=test_user)
+    room = baker.make(Room, youtube_status=3)
+    question = baker.make(Question, answered=False, room=room)
+    baker.make(UpDownVote, question=question, user=test_user)
     html = vote_action(question, test_user.username)
 
     assert 'disabled' in html
@@ -42,9 +50,9 @@
 
 @pytest.mark.django_db
 def test_vote_action_enabled_same_user(test_user):
-    room = mixer.blend(Room, youtube_status=1)
-    question = mixer.blend(Question, answered=False, room=room)
-    mixer.blend(UpDownVote, question=question, user=test_user)
+    room = baker.make(Room, youtube_status=1)
+    question = baker.make(Question, answered=False, room=room)
+    baker.make(UpDownVote, question=question, user=test_user)
     html = vote_action(question, test_user.username)
 
     assert 'voted' in html
@@ -53,8 +61,8 @@
 
 @pytest.mark.django_db
 def test_vote_action_disabled_diferent_user(test_user):
-    room = mixer.blend(Room, youtube_status=2)
-    question = mixer.blend(Question, answered=False, room=room)
+    room = baker.make(Room, youtube_status=3)
+    question = baker.make(Question, answered=False, room=room)
     html = vote_action(question, test_user.username)
 
     assert 'disabled' in html
@@ -63,8 +71,8 @@
 
 @pytest.mark.django_db
 def test_vote_action_enabled_diferent_user(test_user):
-    room = mixer.blend(Room, youtube_status=1)
-    question = mixer.blend(Question, answered=False, room=room)
+    room = baker.make(Room, youtube_status=1)
+    question = baker.make(Question, answered=False, room=room)
     html = vote_action(question, test_user.username)
 
     assert 'JS-voteBtnEnabled' in html
@@ -85,7 +93,7 @@
 
 @pytest.mark.django_db
 def test_belongs_to_group_exists(test_user):
-    group = mixer.blend(Group, name='testgroup')
+    group = baker.make(Group, name='testgroup')
     test_user.groups.add(group)
     has_group = belongs_to_group(test_user, 'testgroup')
 
@@ -94,8 +102,7 @@
 
 @pytest.mark.django_db
 def test_belongs_to_group_is_adamin(test_user):
-    mixer.blend(UserProfile, is_admin=True, user=test_user)
+    baker.make(UserProfile, is_admin=True, user=test_user)
     has_group = belongs_to_group(test_user, 'testgroup')
 
     assert has_group == True
-
diff -ruN a/apps/core/tests/test_views.py b/apps/core/tests/test_views.py
--- a/apps/core/tests/test_views.py	2026-05-15 16:14:49.000000000 -0300
+++ b/apps/core/tests/test_views.py	2026-05-26 09:04:21.577307744 -0300
@@ -1,7 +1,7 @@
 import pytest
 from apps.core.models import Question, Room, UpDownVote, Video, RoomAttachment
 from apps.accounts.models import UserProfile
-from mixer.backend.django import mixer
+from model_bakery import baker
 from django.urls import reverse
 from django.test import RequestFactory
 import json
@@ -13,9 +13,14 @@
         username='testuser', email='test@e.com', is_active=True)
 
 
+def make_question(**kwargs):
+    kwargs.setdefault('_fill_optional', True)
+    return baker.make(Question, **kwargs)
+
+
 @pytest.mark.django_db
 def test_redirect_to_room(client):
-    room = mixer.blend(Room, cod_reunion='12345', is_visible=True)
+    room = baker.make(Room, cod_reunion='12345', is_visible=True)
     url = reverse(
         'video_reunion_room', kwargs={'cod_reunion': room.cod_reunion})
     response = client.get(url)
@@ -34,10 +39,10 @@
 
 @pytest.mark.django_db
 def test_set_answer_time_with_time(client, test_user):
-    mixer.blend(UserProfile, is_admin=True, user=test_user)
-    question = mixer.blend(Question)
-    video = mixer.blend(Video, video_id='testId')
-    mixer.blend(UpDownVote, question=question)
+    baker.make(UserProfile, is_admin=True, user=test_user)
+    question = make_question()
+    video = baker.make(Video, video_id='testId')
+    baker.make(UpDownVote, question=question)
 
     data = {'answer_time': '10', 'video_id': video.video_id}
     url = reverse('set_question_answer_time', kwargs={
@@ -51,9 +56,9 @@
 
 @pytest.mark.django_db
 def test_set_answer_time_without_time(client, test_user):
-    mixer.blend(UserProfile, is_admin=True, user=test_user)
-    question = mixer.blend(Question)
-    video = mixer.blend(Video, video_id='testId')
+    baker.make(UserProfile, is_admin=True, user=test_user)
+    question = make_question()
+    video = baker.make(Video, video_id='testId')
 
     data = {'answer_time': '0', 'video_id': video.video_id}
     url = reverse('set_question_answer_time', kwargs={
@@ -67,8 +72,8 @@
 
 @pytest.mark.django_db
 def test_set_answer_time_user_no_group(client, test_user):
-    question = mixer.blend(Question)
-    video = mixer.blend(Video, video_id='testId')
+    question = make_question()
+    video = baker.make(Video, video_id='testId')
 
     data = {'answer_time': '0', 'video_id': video.video_id}
     url = reverse('set_question_answer_time', kwargs={
@@ -82,8 +87,8 @@
 
 @pytest.mark.django_db
 def test_set_answer_time_no_answer(client, test_user):
-    question = mixer.blend(Question)
-    video = mixer.blend(Video, video_id='testId')
+    question = make_question()
+    video = baker.make(Video, video_id='testId')
 
     data = {'video_id': video.video_id}
     url = reverse('set_question_answer_time', kwargs={
@@ -97,8 +102,8 @@
 
 @pytest.mark.django_db
 def test_set_answer_time_unauthenticated(client):
-    question = mixer.blend(Question)
-    video = mixer.blend(Video, video_id='testId')
+    question = make_question()
+    video = baker.make(Video, video_id='testId')
 
     data = {'video_id': video.video_id}
     url = reverse('set_question_answer_time', kwargs={
@@ -111,9 +116,9 @@
 
 @pytest.mark.django_db
 def test_set_answered(client, test_user):
-    mixer.blend(UserProfile, is_admin=True, user=test_user)
-    question = mixer.blend(Question)
-    mixer.blend(UpDownVote, question=question)
+    baker.make(UserProfile, is_admin=True, user=test_user)
+    question = make_question()
+    baker.make(UpDownVote, question=question)
 
     data = {'answered': 'true'}
     url = reverse('set_question_answered', kwargs={
@@ -127,9 +132,9 @@
 
 @pytest.mark.django_db
 def test_set_answered_false(client, test_user):
-    mixer.blend(UserProfile, is_admin=True, user=test_user)
-    question = mixer.blend(Question)
-    mixer.blend(UpDownVote, question=question)
+    baker.make(UserProfile, is_admin=True, user=test_user)
+    question = make_question()
+    baker.make(UpDownVote, question=question)
 
     data = {'answered': 'false'}
     url = reverse('set_question_answered', kwargs={
@@ -143,7 +148,7 @@
 
 @pytest.mark.django_db
 def test_set_answered_unauthorizated(client, test_user):
-    question = mixer.blend(Question)
+    question = make_question()
 
     url = reverse('set_question_answered', kwargs={
         'question_id': question.id})
@@ -156,7 +161,7 @@
 
 @pytest.mark.django_db
 def test_set_answered_unauthenticated(client):
-    question = mixer.blend(Question)
+    question = make_question()
 
     url = reverse('set_question_answered', kwargs={
         'question_id': question.id})
@@ -168,9 +173,9 @@
 
 @pytest.mark.django_db
 def test_set_priotity(client, test_user):
-    mixer.blend(UserProfile, is_admin=True, user=test_user)
-    question = mixer.blend(Question)
-    mixer.blend(UpDownVote, question=question)
+    baker.make(UserProfile, is_admin=True, user=test_user)
+    question = make_question()
+    baker.make(UpDownVote, question=question)
 
     data = {'is_priority': 'true'}
     url = reverse('set_question_priotity', kwargs={
@@ -184,9 +189,9 @@
 
 @pytest.mark.django_db
 def test_set_priotity_false(client, test_user):
-    mixer.blend(UserProfile, is_admin=True, user=test_user)
-    question = mixer.blend(Question)
-    mixer.blend(UpDownVote, question=question)
+    baker.make(UserProfile, is_admin=True, user=test_user)
+    question = make_question()
+    baker.make(UpDownVote, question=question)
 
     data = {'is_priority': 'false'}
     url = reverse('set_question_priotity', kwargs={
@@ -200,7 +205,7 @@
 
 @pytest.mark.django_db
 def test_set_priotity_unauthorizated(client, test_user):
-    question = mixer.blend(Question)
+    question = make_question()
 
     url = reverse('set_question_priotity', kwargs={
                   'question_id': question.id})
@@ -213,7 +218,7 @@
 
 @pytest.mark.django_db
 def test_set_priotity_unauthenticated(client):
-    question = mixer.blend(Question)
+    question = make_question()
 
     url = reverse('set_question_priotity', kwargs={
         'question_id': question.id})
@@ -225,15 +230,15 @@
 
 @pytest.mark.django_db
 def test_index(client):
-    response = client.get('/')
+    response = client.get(reverse('home'))
 
     assert response.status_code == 200
 
 
 @pytest.mark.django_db
 def test_create_attachment(client, test_user):
-    room = mixer.blend(Room)
-    mixer.blend(UserProfile, is_admin=True, user=test_user)
+    room = baker.make(Room)
+    baker.make(UserProfile, is_admin=True, user=test_user)
     client.force_login(test_user)
 
     data = {'url': 'test.com', 'title': 'test link'}
@@ -248,7 +253,7 @@
 
 @pytest.mark.django_db
 def test_create_attachment_unauthorizated(client, test_user):
-    room = mixer.blend(Room)
+    room = baker.make(Room)
 
     url = reverse('create_attachment',
                   kwargs={'room_id': room.id})
@@ -261,7 +266,7 @@
 
 @pytest.mark.django_db
 def test_create_attachment_unauthenticated(client):
-    room = mixer.blend(Room)
+    room = baker.make(Room)
 
     url = reverse('create_attachment',
                   kwargs={'room_id': room.id})
@@ -273,8 +278,8 @@
 
 @pytest.mark.django_db
 def test_delete_attachment(client, test_user):
-    attachment = mixer.blend(RoomAttachment)
-    mixer.blend(UserProfile, is_admin=True, user=test_user)
+    attachment = baker.make(RoomAttachment)
+    baker.make(UserProfile, is_admin=True, user=test_user)
     client.force_login(test_user)
 
     url = reverse('delete_attachment',
@@ -287,7 +292,7 @@
 
 @pytest.mark.django_db
 def test_delete_attachment_unauthorizated(client, test_user):
-    attachment = mixer.blend(RoomAttachment)
+    attachment = baker.make(RoomAttachment)
 
     url = reverse('delete_attachment',
                   kwargs={'attachment_id': attachment.id})
@@ -300,7 +305,7 @@
 
 @pytest.mark.django_db
 def test_delete_attachment_unauthenticated(client):
-    attachment = mixer.blend(RoomAttachment)
+    attachment = baker.make(RoomAttachment)
 
     url = reverse('delete_attachment',
                   kwargs={'attachment_id': attachment.id})
@@ -312,8 +317,8 @@
 
 @pytest.mark.django_db
 def test_add_external_link(client, test_user):
-    room = mixer.blend(Room)
-    mixer.blend(UserProfile, is_admin=True, user=test_user)
+    room = baker.make(Room)
+    baker.make(UserProfile, is_admin=True, user=test_user)
     client.force_login(test_user)
 
     url = reverse('add_external_link',
@@ -326,7 +331,7 @@
 
 @pytest.mark.django_db
 def test_add_external_link_unauthorizated(client, test_user):
-    room = mixer.blend(Room)
+    room = baker.make(Room)
 
     url = reverse('add_external_link',
                   kwargs={'room_id': room.id})
@@ -339,7 +344,7 @@
 
 @pytest.mark.django_db
 def test_add_external_link_unauthenticated(client):
-    room = mixer.blend(Room)
+    room = baker.make(Room)
 
     url = reverse('add_external_link',
                   kwargs={'room_id': room.id})
@@ -351,8 +356,8 @@
 
 @pytest.mark.django_db
 def test_remove_external_link(client, test_user):
-    room = mixer.blend(Room)
-    mixer.blend(UserProfile, is_admin=True, user=test_user)
+    room = baker.make(Room)
+    baker.make(UserProfile, is_admin=True, user=test_user)
     client.force_login(test_user)
 
     url = reverse('remove_external_link',
@@ -365,7 +370,7 @@
 
 @pytest.mark.django_db
 def test_remove_external_link_unauthorizated(client, test_user):
-    room = mixer.blend(Room)
+    room = baker.make(Room)
 
     url = reverse('remove_external_link',
                   kwargs={'room_id': room.id})
@@ -378,7 +383,7 @@
 
 @pytest.mark.django_db
 def test_remove_external_link_unauthenticated(client):
-    room = mixer.blend(Room)
+    room = baker.make(Room)
 
     url = reverse('remove_external_link',
                   kwargs={'room_id': room.id})
@@ -390,8 +395,8 @@
 
 @pytest.mark.django_db
 def test_create_video_attachment(client, test_user):
-    room = mixer.blend(Room)
-    mixer.blend(UserProfile, is_admin=True, user=test_user)
+    room = baker.make(Room)
+    baker.make(UserProfile, is_admin=True, user=test_user)
     client.force_login(test_user)
 
     data = {'video_id': 'testID', 'title': 'testTitle'}
@@ -406,7 +411,7 @@
 
 @pytest.mark.django_db
 def test_create_video_attachment_unauthorizated(client, test_user):
-    room = mixer.blend(Room)
+    room = baker.make(Room)
 
     url = reverse('create_video_attachment',
                   kwargs={'room_id': room.id})
@@ -419,7 +424,7 @@
 
 @pytest.mark.django_db
 def test_create_video_attachment_unauthenticated(client):
-    room = mixer.blend(Room)
+    room = baker.make(Room)
 
     url = reverse('create_video_attachment',
                   kwargs={'room_id': room.id})
@@ -431,8 +436,8 @@
 
 @pytest.mark.django_db
 def test_delete_video(client, test_user):
-    video = mixer.blend(Video)
-    mixer.blend(UserProfile, is_admin=True, user=test_user)
+    video = baker.make(Video)
+    baker.make(UserProfile, is_admin=True, user=test_user)
     client.force_login(test_user)
 
     url = reverse('delete_video',
@@ -445,7 +450,7 @@
 
 @pytest.mark.django_db
 def test_delete_video_unauthorizated(client, test_user):
-    video = mixer.blend(Video)
+    video = baker.make(Video)
 
     url = reverse('delete_video',
                   kwargs={'video_id': video.id})
@@ -458,7 +463,7 @@
 
 @pytest.mark.django_db
 def test_delete_video_unauthenticated(client):
-    video = mixer.blend(Video)
+    video = baker.make(Video)
 
     url = reverse('delete_video',
                   kwargs={'video_id': video.id})
@@ -470,9 +475,9 @@
 
 @pytest.mark.django_db
 def test_order_videos(test_user, client):
-    room = mixer.blend(Room)
-    video = mixer.blend(Video, video_id='testVideo', order=1)
-    mixer.blend(UserProfile, is_admin=True, user=test_user)
+    room = baker.make(Room)
+    video = baker.make(Video, video_id='testVideo', order=1)
+    baker.make(UserProfile, is_admin=True, user=test_user)
     client.force_login(test_user)
 
     data = {'data': json.dumps([{'id': video.id, 'order': 2}])}
@@ -491,7 +496,7 @@
 
 @pytest.mark.django_db
 def test_order_videos_unauthorizated(client, test_user):
-    room = mixer.blend(Room)
+    room = baker.make(Room)
 
     url = reverse('order_videos',
                   kwargs={'room_id': room.id})
@@ -504,7 +509,7 @@
 
 @pytest.mark.django_db
 def test_order_videos_unauthenticated(client):
-    room = mixer.blend(Room)
+    room = baker.make(Room)
 
     url = reverse('order_videos',
                   kwargs={'room_id': room.id})
@@ -516,7 +521,7 @@
 
 @pytest.mark.django_db
 def test_video_detail(client, test_user):
-    room = mixer.blend(Room, is_active=True)
+    room = baker.make(Room, is_active=True)
 
     url = reverse('video_room',
                   kwargs={'pk': room.id})
@@ -529,7 +534,7 @@
 
 @pytest.mark.django_db
 def test_video_detail_inactive(client, test_user):
-    room = mixer.blend(Room, is_active=False)
+    room = baker.make(Room, is_active=False)
 
     url = reverse('video_room',
                   kwargs={'pk': room.id})
@@ -542,7 +547,7 @@
 
 @pytest.mark.django_db
 def test_widget(client, test_user):
-    room = mixer.blend(Room, is_active=True)
+    room = baker.make(Room, is_active=True)
 
     url = reverse('widget_index',
                   kwargs={'pk': room.id})
@@ -555,7 +560,7 @@
 
 @pytest.mark.django_db
 def test_widget_inactive(client, test_user):
-    room = mixer.blend(Room, is_active=False)
+    room = baker.make(Room, is_active=False)
 
     url = reverse('widget_index',
                   kwargs={'pk': room.id})
@@ -568,7 +573,7 @@
 
 @pytest.mark.django_db
 def test_room_report(client, test_user):
-    room = mixer.blend(Room, is_active=True)
+    room = baker.make(Room, is_active=True)
 
     url = reverse('room_report',
                   kwargs={'pk': room.id})
@@ -581,7 +586,7 @@
 
 @pytest.mark.django_db
 def test_room_report_inactive(client, test_user):
-    room = mixer.blend(Room, is_active=False)
+    room = baker.make(Room, is_active=False)
 
     url = reverse('room_report',
                   kwargs={'pk': room.id})
@@ -594,7 +599,7 @@
 
 @pytest.mark.django_db
 def test_closed_videos(client):
-    mixer.blend(Room,
+    baker.make(Room,
         title_reunion='test',
         date='2022-01-01',
         is_visible=True,
@@ -611,8 +616,8 @@
 
 @pytest.mark.django_db
 def test_room_question_list(client, test_user):
-    room = mixer.blend(Room, is_active=True)
-    mixer.blend(UserProfile, is_admin=True, user=test_user)
+    room = baker.make(Room, is_active=True)
+    baker.make(UserProfile, is_admin=True, user=test_user)
 
     url = reverse('questions_list', kwargs={'pk': room.id})
 
@@ -624,7 +629,7 @@
 
 @pytest.mark.django_db
 def test_room_question_list_unauthorized(client, test_user):
-    room = mixer.blend(Room, is_active=True)
+    room = baker.make(Room, is_active=True)
 
     url = reverse('questions_list', kwargs={'pk': room.id})
 
@@ -636,7 +641,7 @@
 
 @pytest.mark.django_db
 def test_question_detail(client):
-    question = mixer.blend(Question)
+    question = make_question()
 
     url = reverse('question_detail',
                   kwargs={'pk': question.id})
@@ -671,4 +676,4 @@
 
     response = client.get(url)
 
-    assert 'pqp' in str(response.content)
\ No newline at end of file
+    assert 'pqp' in str(response.content)
diff -ruN a/apps/notification/tests/test_notifications.py b/apps/notification/tests/test_notifications.py
--- a/apps/notification/tests/test_notifications.py	2026-05-15 16:14:49.000000000 -0300
+++ b/apps/notification/tests/test_notifications.py	2026-05-26 08:48:34.676063459 -0300
@@ -1,5 +1,5 @@
 import pytest
-from mixer.backend.django import mixer
+from model_bakery import baker
 from apps.notification.models import ParticipantNotification
 from apps.core.models import Room, Message
 from apps.accounts.models import User, UserProfile
@@ -15,24 +15,24 @@
 
     @pytest.mark.django_db
     def test_notification_create(self):
-        room = mixer.blend(Room, title_reunion='Title test')
-        notification = mixer.blend(ParticipantNotification, room=room)
+        room = baker.make(Room, title_reunion='Title test')
+        notification = baker.make(ParticipantNotification, room=room)
         assert ParticipantNotification.objects.count() == 1
         assert notification.__str__() == 'Title test'
 
     @pytest.mark.django_db
     def test_delete_room_with_notification(self):
-        room = mixer.blend(Room, title_reunion='Title test')
-        mixer.blend(ParticipantNotification, room=room)
+        room = baker.make(Room, title_reunion='Title test')
+        baker.make(ParticipantNotification, room=room)
         room.delete()
         assert ParticipantNotification.objects.count() == 0
     
     @pytest.mark.django_db
     def test_send_participants_notification(self, client, mailoutbox):
-        room = mixer.blend(Room, is_active=True)
-        mixer.blend(Message, room=room)
+        room = baker.make(Room, is_active=True)
+        baker.make(Message, room=room)
 
-        user = mixer.blend(User, is_active=True)
+        user = baker.make(User, is_active=True)
         UserProfile.objects.create(user=user, is_admin=True)
         
         settings.EMAIL_BACKEND = 'django.core.mail.backends.locmem.EmailBackend'
@@ -47,8 +47,8 @@
 
     @pytest.mark.django_db
     def test_send_participants_notification_without_permission(self, client):
-        room = mixer.blend(Room, is_active=True)
-        user = mixer.blend(User, is_active=True)
+        room = baker.make(Room, is_active=True)
+        user = baker.make(User, is_active=True)
 
         client.force_login(user)
         url = reverse('participants_notification', args=[room.id])
diff -ruN a/apps/reports/tests/test_messages.py b/apps/reports/tests/test_messages.py
--- a/apps/reports/tests/test_messages.py	2026-05-15 16:14:49.000000000 -0300
+++ b/apps/reports/tests/test_messages.py	2026-05-26 09:04:25.580323575 -0300
@@ -1,5 +1,5 @@
 import pytest
-from mixer.backend.django import mixer
+from model_bakery import baker, seq
 from apps.reports.models import MessagesReport
 from apps.core.models import Message
 from django.db import IntegrityError
@@ -16,16 +16,16 @@
 class TestMessagesReport():
     @pytest.mark.django_db
     def test_messages_create(self):
-        messages = mixer.blend(MessagesReport)
+        messages = baker.make(MessagesReport)
         assert MessagesReport.objects.count() == 1
         assert messages.__str__() == ('{} - {}').format(
             messages.start_date.strftime("%d/%m/%Y"), messages.period)
 
     @pytest.mark.django_db
     def test_messages_integrity_error(self):
-        content = mixer.blend(MessagesReport)
+        content = baker.make(MessagesReport)
         with pytest.raises(IntegrityError) as excinfo:
-            mixer.blend(MessagesReport,
+            baker.make(MessagesReport,
                         period=content.period,
                         start_date=content.start_date)
         ## sqlite3 message error
@@ -76,7 +76,7 @@
     def test_get_messages_daily_without_args(self):
         yesterday = datetime.now() - timedelta(days=1)
 
-        message = mixer.blend(Message)
+        message = baker.make(Message)
         message.created = yesterday
         message.save()
 
@@ -93,7 +93,7 @@
     @pytest.mark.django_db
     def test_get_messages_monthly_without_args(self):
         yesterday = date.today() - timedelta(days=1)
-        mixer.blend(MessagesReport, period='daily', messages=10,
+        baker.make(MessagesReport, period='daily', messages=10,
                     start_date=yesterday, end_date=yesterday)
 
         get_messages_monthly.apply()
@@ -109,7 +109,7 @@
     @pytest.mark.django_db
     def test_get_messages_yearly_without_args(self):
         yesterday = date.today() - timedelta(days=1)
-        mixer.blend(MessagesReport, period='monthly', messages=10,
+        baker.make(MessagesReport, period='monthly', messages=10,
                     start_date=yesterday.replace(day=1),
                     end_date=yesterday)
 
@@ -125,10 +125,10 @@
     @pytest.mark.django_db
     def test_get_messages_yearly_current_year(self):
         yesterday = date.today() - timedelta(days=1)
-        mixer.blend(MessagesReport, period='monthly', messages=10,
+        baker.make(MessagesReport, period='monthly', messages=10,
                     start_date=yesterday.replace(day=1),
                     end_date=yesterday)
-        mixer.blend(MessagesReport, period='yearly', messages=9,
+        baker.make(MessagesReport, period='yearly', messages=9,
                     start_date=yesterday.replace(day=1, month=1),
                     end_date=yesterday - timedelta(days=1))
 
@@ -143,7 +143,11 @@
 
     @pytest.mark.django_db
     def test_messages_api_url(api_client):
-        mixer.cycle(5).blend(MessagesReport)
+        baker.make(
+            MessagesReport,
+            start_date=seq(date(2020, 1, 1), timedelta(days=1)),
+            end_date=seq(date(2020, 1, 1), timedelta(days=1)),
+            _quantity=5)
         url = reverse('messagesreport-list')
         client = APIClient()
         response = client.get(url)
diff -ruN a/apps/reports/tests/test_newusers.py b/apps/reports/tests/test_newusers.py
--- a/apps/reports/tests/test_newusers.py	2026-05-15 16:14:49.000000000 -0300
+++ b/apps/reports/tests/test_newusers.py	2026-05-26 09:04:49.557418220 -0300
@@ -1,5 +1,5 @@
 import pytest
-from mixer.backend.django import mixer
+from model_bakery import baker, seq
 from apps.reports.models import NewUsers
 from django.db import IntegrityError
 from apps.reports.tasks import (create_new_users_object,
@@ -20,16 +20,16 @@
 
     @pytest.mark.django_db
     def test_new_users_create(self):
-        new_users = mixer.blend(NewUsers)
+        new_users = baker.make(NewUsers)
         assert NewUsers.objects.count() == 1
         assert new_users.__str__() == ('{} - {}').format(
             new_users.start_date.strftime("%d/%m/%Y"), new_users.period)
 
     @pytest.mark.django_db
     def test_new_users_integrity_error(self):
-        content = mixer.blend(NewUsers)
+        content = baker.make(NewUsers)
         with pytest.raises(IntegrityError) as excinfo:
-            mixer.blend(NewUsers,
+            baker.make(NewUsers,
                         period=content.period,
                         start_date=content.start_date)
         ## sqlite3 message error
@@ -78,7 +78,7 @@
 
     @pytest.mark.django_db
     def test_get_new_users_daily_with_args(self):
-        mixer.blend(get_user_model(), date_joined='2020-10-01')
+        baker.make(get_user_model(), date_joined='2020-10-01')
 
         get_new_users_daily.apply(args=(['2020-10-01']))
 
@@ -93,7 +93,7 @@
     @pytest.mark.django_db
     def test_get_new_users_daily_without_args(self):
         yesterday = date.today() - timedelta(days=1)
-        user = mixer.blend(get_user_model(), date_joined=yesterday)
+        user = baker.make(get_user_model(), date_joined=yesterday)
 
         get_new_users_daily.apply()
 
@@ -107,10 +107,10 @@
 
     @pytest.mark.django_db
     def test_get_new_users_monthly_with_args(self):
-        mixer.cycle(5).blend(NewUsers, period='daily',
-                             new_users=10, start_date=mixer.sequence(
-                                 '2020-10-1{0}'),
-                             end_date=mixer.sequence('2020-10-1{0}'))
+        baker.make(NewUsers, period='daily', new_users=10,
+                   start_date=seq(date(2020, 10, 10), timedelta(days=1)),
+                   end_date=seq(date(2020, 10, 10), timedelta(days=1)),
+                   _quantity=5)
 
         get_new_users_monthly.apply(args=(['2020-10-01']))
 
@@ -125,7 +125,7 @@
     @pytest.mark.django_db
     def test_get_new_users_monthly_without_args(self):
         yesterday = date.today() - timedelta(days=1)
-        mixer.blend(NewUsers, period='daily', new_users=10,
+        baker.make(NewUsers, period='daily', new_users=10,
                     start_date=yesterday, end_date=yesterday)
 
         get_new_users_monthly.apply()
@@ -143,7 +143,7 @@
         start_dates = ['2019-01-01', '2019-02-01', '2019-03-01']
         end_dates = ['2019-01-31', '2019-02-28', '2019-03-31']
         for i in range(3):
-            mixer.blend(NewUsers, period='monthly', new_users=10,
+            baker.make(NewUsers, period='monthly', new_users=10,
                         start_date=start_dates[i], end_date=end_dates[i])
 
         get_new_users_yearly.apply(args=(['2019-01-01']))
@@ -159,7 +159,7 @@
     @pytest.mark.django_db
     def test_get_new_users_yearly_without_args(self):
         yesterday = date.today() - timedelta(days=1)
-        mixer.blend(NewUsers, period='monthly', new_users=10,
+        baker.make(NewUsers, period='monthly', new_users=10,
                     start_date=yesterday.replace(day=1),
                     end_date=yesterday)
 
@@ -175,10 +175,10 @@
     @pytest.mark.django_db
     def test_get_new_users_yearly_current_year(self):
         yesterday = date.today() - timedelta(days=1)
-        mixer.blend(NewUsers, period='monthly', new_users=10,
+        baker.make(NewUsers, period='monthly', new_users=10,
                     start_date=yesterday.replace(day=1),
                     end_date=yesterday)
-        mixer.blend(NewUsers, period='yearly', new_users=9,
+        baker.make(NewUsers, period='yearly', new_users=9,
                     start_date=yesterday.replace(day=1, month=1),
                     end_date=yesterday - timedelta(days=1))
 
@@ -201,7 +201,11 @@
 
     @pytest.mark.django_db
     def test_new_users_api_url(api_client):
-        mixer.cycle(5).blend(NewUsers)
+        baker.make(
+            NewUsers,
+            start_date=seq(date(2020, 1, 1), timedelta(days=1)),
+            end_date=seq(date(2020, 1, 1), timedelta(days=1)),
+            _quantity=5)
         url = reverse('newusers-list')
         client = APIClient()
         response = client.get(url)
diff -ruN a/apps/reports/tests/test_participants.py b/apps/reports/tests/test_participants.py
--- a/apps/reports/tests/test_participants.py	2026-05-15 16:14:49.000000000 -0300
+++ b/apps/reports/tests/test_participants.py	2026-05-26 09:04:34.541358982 -0300
@@ -1,5 +1,5 @@
 import pytest
-from mixer.backend.django import mixer
+from model_bakery import baker, seq
 from apps.reports.models import ParticipantsReport
 from django.db import IntegrityError
 from apps.core.models import Message, Question, Room, UpDownVote
@@ -17,16 +17,16 @@
 class TestParticipantsReport():
     @pytest.mark.django_db
     def test_participants_create(self):
-        participants = mixer.blend(ParticipantsReport)
+        participants = baker.make(ParticipantsReport)
         assert ParticipantsReport.objects.count() == 1
         assert participants.__str__() == ('{} - {}').format(
             participants.start_date.strftime("%d/%m/%Y"), participants.period)
 
     @pytest.mark.django_db
     def test_participants_integrity_error(self):
-        content = mixer.blend(ParticipantsReport)
+        content = baker.make(ParticipantsReport)
         with pytest.raises(IntegrityError) as excinfo:
-            mixer.blend(ParticipantsReport,
+            baker.make(ParticipantsReport,
                         period=content.period,
                         start_date=content.start_date)
         ## sqlite3 message error
@@ -72,7 +72,7 @@
     def test_get_participants_daily_without_args(self):
         yesterday = datetime.now() - timedelta(days=1)
 
-        message = mixer.blend(Message)
+        message = baker.make(Message)
         message.created = yesterday
         message.save()
 
@@ -89,7 +89,7 @@
     @pytest.mark.django_db
     def test_get_participants_monthly_without_args(self):
         yesterday = date.today() - timedelta(days=1)
-        message = mixer.blend(Message)
+        message = baker.make(Message)
         message.created = yesterday
         message.save()
 
@@ -106,7 +106,7 @@
     @pytest.mark.django_db
     def test_get_participants_yearly_without_args(self):
         yesterday = date.today() - timedelta(days=1)
-        message = mixer.blend(Message)
+        message = baker.make(Message)
         message.created = yesterday
         message.save()
 
@@ -123,11 +123,11 @@
     @pytest.mark.django_db
     def test_get_participants_yearly_current_year(self):
         yesterday = date.today() - timedelta(days=1)
-        message = mixer.blend(Message)
+        message = baker.make(Message)
         message.created = yesterday
         message.save()
 
-        mixer.blend(ParticipantsReport, period='yearly', participants=0,
+        baker.make(ParticipantsReport, period='yearly', participants=0,
                     start_date=yesterday.replace(day=1, month=1),
                     end_date=yesterday - timedelta(days=1))
 
@@ -145,13 +145,13 @@
         yesterday = date.today() - timedelta(days=1)
         initial_date = date(year=2020, month=1, day=1)
 
-        first_room = mixer.blend(Room, date=initial_date)
+        first_room = baker.make(Room, date=initial_date)
         first_room.created = initial_date
         first_room.save()
 
         ParticipantsReport.objects.all().delete()
 
-        mixer.blend(ParticipantsReport, period='all', participants=0,
+        baker.make(ParticipantsReport, period='all', participants=0,
                     start_date=initial_date,
                     end_date=yesterday)
 
@@ -160,7 +160,7 @@
         UpDownVote.objects.all().delete()
         Question.objects.all().delete()
 
-        message = mixer.blend(Message, room=first_room)
+        message = baker.make(Message, room=first_room)
         message.created = yesterday
         message.save()
 
@@ -175,7 +175,11 @@
 
     @pytest.mark.django_db
     def test_participants_api_url(api_client):
-        mixer.cycle(5).blend(ParticipantsReport)
+        baker.make(
+            ParticipantsReport,
+            start_date=seq(date(2020, 1, 1), timedelta(days=1)),
+            end_date=seq(date(2020, 1, 1), timedelta(days=1)),
+            _quantity=5)
         url = reverse('participantsreport-list')
         client = APIClient()
         response = client.get(url)
diff -ruN a/apps/reports/tests/test_questions.py b/apps/reports/tests/test_questions.py
--- a/apps/reports/tests/test_questions.py	2026-05-15 16:14:49.000000000 -0300
+++ b/apps/reports/tests/test_questions.py	2026-05-26 09:04:38.410374257 -0300
@@ -1,5 +1,5 @@
 import pytest
-from mixer.backend.django import mixer
+from model_bakery import baker, seq
 from apps.reports.models import QuestionsReport
 from apps.core.models import Question, Room
 from django.db import IntegrityError
@@ -16,16 +16,16 @@
 class TestQuestionsReport():
     @pytest.mark.django_db
     def test_questions_create(self):
-        questions = mixer.blend(QuestionsReport)
+        questions = baker.make(QuestionsReport)
         assert QuestionsReport.objects.count() == 1
         assert questions.__str__() == ('{} - {}').format(
             questions.start_date.strftime("%d/%m/%Y"), questions.period)
 
     @pytest.mark.django_db
     def test_questions_integrity_error(self):
-        content = mixer.blend(QuestionsReport)
+        content = baker.make(QuestionsReport)
         with pytest.raises(IntegrityError) as excinfo:
-            mixer.blend(QuestionsReport,
+            baker.make(QuestionsReport,
                         period=content.period,
                         start_date=content.start_date)
         ## sqlite3 message error
@@ -76,7 +76,7 @@
     def test_get_questions_daily_without_args(self):
         yesterday = datetime.now() - timedelta(days=1)
 
-        question = mixer.blend(Question)
+        question = baker.make(Question, room=baker.make(Room))
         question.created = yesterday
         question.save()
 
@@ -93,7 +93,7 @@
     @pytest.mark.django_db
     def test_get_questions_monthly_without_args(self):
         yesterday = date.today() - timedelta(days=1)
-        mixer.blend(QuestionsReport, period='daily', questions=10,
+        baker.make(QuestionsReport, period='daily', questions=10,
                     start_date=yesterday, end_date=yesterday)
 
         get_questions_monthly.apply()
@@ -109,7 +109,7 @@
     @pytest.mark.django_db
     def test_get_questions_yearly_without_args(self):
         yesterday = date.today() - timedelta(days=1)
-        mixer.blend(QuestionsReport, period='monthly', questions=10,
+        baker.make(QuestionsReport, period='monthly', questions=10,
                     start_date=yesterday.replace(day=1),
                     end_date=yesterday)
 
@@ -125,10 +125,10 @@
     @pytest.mark.django_db
     def test_get_questions_yearly_current_year(self):
         yesterday = date.today() - timedelta(days=1)
-        mixer.blend(QuestionsReport, period='monthly', questions=10,
+        baker.make(QuestionsReport, period='monthly', questions=10,
                     start_date=yesterday.replace(day=1),
                     end_date=yesterday)
-        mixer.blend(QuestionsReport, period='yearly', questions=9,
+        baker.make(QuestionsReport, period='yearly', questions=9,
                     start_date=yesterday.replace(day=1, month=1),
                     end_date=yesterday - timedelta(days=1))
 
@@ -143,7 +143,11 @@
 
     @pytest.mark.django_db
     def test_questions_api_url(api_client):
-        mixer.cycle(5).blend(QuestionsReport)
+        baker.make(
+            QuestionsReport,
+            start_date=seq(date(2020, 1, 1), timedelta(days=1)),
+            end_date=seq(date(2020, 1, 1), timedelta(days=1)),
+            _quantity=5)
         url = reverse('questionsreport-list')
         client = APIClient()
         response = client.get(url)
diff -ruN a/apps/reports/tests/test_ranking.py b/apps/reports/tests/test_ranking.py
--- a/apps/reports/tests/test_ranking.py	2026-05-15 16:14:49.000000000 -0300
+++ b/apps/reports/tests/test_ranking.py	2026-05-26 08:49:29.468356721 -0300
@@ -1,5 +1,5 @@
 import pytest
-from mixer.backend.django import mixer
+from model_bakery import baker
 from apps.core.models import Room
 from django.urls import reverse
 import json
@@ -11,7 +11,7 @@
     @pytest.mark.django_db
     def test_ranking_api_url(api_client):
         yesterday = date.today() - timedelta(days=1)
-        mixer.cycle(5).blend(Room, date=yesterday)
+        baker.make(Room, date=yesterday, _quantity=5)
         url = reverse('ranking-list')
         client = APIClient()
         response = client.get(url)
diff -ruN a/apps/reports/tests/test_rooms.py b/apps/reports/tests/test_rooms.py
--- a/apps/reports/tests/test_rooms.py	2026-05-15 16:14:49.000000000 -0300
+++ b/apps/reports/tests/test_rooms.py	2026-05-26 09:04:30.428342736 -0300
@@ -1,5 +1,5 @@
 import pytest
-from mixer.backend.django import mixer
+from model_bakery import baker, seq
 from apps.reports.models import RoomsReport
 from apps.core.models import Room
 from django.db import IntegrityError
@@ -16,16 +16,16 @@
 class TestRoomsReport():
     @pytest.mark.django_db
     def test_rooms_create(self):
-        rooms = mixer.blend(RoomsReport)
+        rooms = baker.make(RoomsReport)
         assert RoomsReport.objects.count() == 1
         assert rooms.__str__() == ('{} - {}').format(
             rooms.start_date.strftime("%d/%m/%Y"), rooms.period)
 
     @pytest.mark.django_db
     def test_rooms_integrity_error(self):
-        content = mixer.blend(RoomsReport)
+        content = baker.make(RoomsReport)
         with pytest.raises(IntegrityError) as excinfo:
-            mixer.blend(RoomsReport,
+            baker.make(RoomsReport,
                         period=content.period,
                         start_date=content.start_date)
         ## sqlite3 message error
@@ -85,7 +85,7 @@
     @pytest.mark.django_db
     def test_get_rooms_daily_without_args(self):
         yesterday = datetime.now() - timedelta(days=1)
-        mixer.blend(Room, is_active=True, is_visible=True, date=yesterday)
+        baker.make(Room, is_active=True, is_visible=True, date=yesterday)
 
         get_rooms_daily.apply()
 
@@ -102,7 +102,7 @@
     @pytest.mark.django_db
     def test_get_rooms_monthly_without_args(self):
         yesterday = date.today() - timedelta(days=1)
-        mixer.blend(RoomsReport, period='daily', total_rooms=10,
+        baker.make(RoomsReport, period='daily', total_rooms=10,
                     finished_rooms=8, canceled_rooms=2,
                     start_date=yesterday, end_date=yesterday)
 
@@ -121,7 +121,7 @@
     @pytest.mark.django_db
     def test_get_rooms_yearly_without_args(self):
         yesterday = date.today() - timedelta(days=1)
-        mixer.blend(RoomsReport, period='monthly', total_rooms=10,
+        baker.make(RoomsReport, period='monthly', total_rooms=10,
                     finished_rooms=8, canceled_rooms=2,
                     start_date=yesterday.replace(day=1),
                     end_date=yesterday)
@@ -140,11 +140,11 @@
     @pytest.mark.django_db
     def test_get_rooms_yearly_current_year(self):
         yesterday = date.today() - timedelta(days=1)
-        mixer.blend(RoomsReport, period='monthly', total_rooms=10,
+        baker.make(RoomsReport, period='monthly', total_rooms=10,
                     finished_rooms=8, canceled_rooms=2,
                     start_date=yesterday.replace(day=1),
                     end_date=yesterday)
-        mixer.blend(RoomsReport, period='yearly', total_rooms=10,
+        baker.make(RoomsReport, period='yearly', total_rooms=10,
                     finished_rooms=8, canceled_rooms=2,
                     start_date=yesterday.replace(day=1, month=1),
                     end_date=yesterday - timedelta(days=1))
@@ -162,7 +162,11 @@
 
     @pytest.mark.django_db
     def test_rooms_api_url(api_client):
-        mixer.cycle(5).blend(RoomsReport)
+        baker.make(
+            RoomsReport,
+            start_date=seq(date(2020, 1, 1), timedelta(days=1)),
+            end_date=seq(date(2020, 1, 1), timedelta(days=1)),
+            _quantity=5)
         url = reverse('roomsreport-list')
         client = APIClient()
         response = client.get(url)
diff -ruN a/apps/reports/tests/test_votes.py b/apps/reports/tests/test_votes.py
--- a/apps/reports/tests/test_votes.py	2026-05-15 16:14:49.000000000 -0300
+++ b/apps/reports/tests/test_votes.py	2026-05-26 09:04:42.469390273 -0300
@@ -1,5 +1,5 @@
 import pytest
-from mixer.backend.django import mixer
+from model_bakery import baker, seq
 from apps.reports.models import VotesReport
 from apps.core.models import UpDownVote, Question, Room
 from django.db import IntegrityError
@@ -16,16 +16,16 @@
 class TestVotesReport():
     @pytest.mark.django_db
     def test_votes_create(self):
-        votes = mixer.blend(VotesReport)
+        votes = baker.make(VotesReport)
         assert VotesReport.objects.count() == 1
         assert votes.__str__() == ('{} - {}').format(
             votes.start_date.strftime("%d/%m/%Y"), votes.period)
 
     @pytest.mark.django_db
     def test_votes_integrity_error(self):
-        content = mixer.blend(VotesReport)
+        content = baker.make(VotesReport)
         with pytest.raises(IntegrityError) as excinfo:
-            mixer.blend(VotesReport,
+            baker.make(VotesReport,
                         period=content.period,
                         start_date=content.start_date)
         ## sqlite3 message error
@@ -75,7 +75,7 @@
     @pytest.mark.django_db
     def test_get_votes_daily_without_args(self):
         yesterday = datetime.now() - timedelta(days=1)
-        vote = mixer.blend(UpDownVote)
+        vote = baker.make(UpDownVote, question__room=baker.make(Room))
         vote.created = yesterday
         vote.save()
 
@@ -92,7 +92,7 @@
     @pytest.mark.django_db
     def test_get_votes_monthly_without_args(self):
         yesterday = date.today() - timedelta(days=1)
-        mixer.blend(VotesReport, period='daily', votes=10,
+        baker.make(VotesReport, period='daily', votes=10,
                     start_date=yesterday, end_date=yesterday)
 
         get_votes_monthly.apply()
@@ -108,7 +108,7 @@
     @pytest.mark.django_db
     def test_get_votes_yearly_without_args(self):
         yesterday = date.today() - timedelta(days=1)
-        mixer.blend(VotesReport, period='monthly', votes=10,
+        baker.make(VotesReport, period='monthly', votes=10,
                     start_date=yesterday.replace(day=1),
                     end_date=yesterday)
 
@@ -124,10 +124,10 @@
     @pytest.mark.django_db
     def test_get_votes_yearly_current_year(self):
         yesterday = date.today() - timedelta(days=1)
-        mixer.blend(VotesReport, period='monthly', votes=10,
+        baker.make(VotesReport, period='monthly', votes=10,
                     start_date=yesterday.replace(day=1),
                     end_date=yesterday)
-        mixer.blend(VotesReport, period='yearly', votes=9,
+        baker.make(VotesReport, period='yearly', votes=9,
                     start_date=yesterday.replace(day=1, month=1),
                     end_date=yesterday - timedelta(days=1))
 
@@ -142,7 +142,11 @@
 
     @pytest.mark.django_db
     def test_votes_api_url(api_client):
-        mixer.cycle(5).blend(VotesReport)
+        baker.make(
+            VotesReport,
+            start_date=seq(date(2020, 1, 1), timedelta(days=1)),
+            end_date=seq(date(2020, 1, 1), timedelta(days=1)),
+            _quantity=5)
         url = reverse('votesreport-list')
         client = APIClient()
         response = client.get(url)
diff -ruN a/audiencias_publicas/settings.py b/audiencias_publicas/settings.py
--- a/audiencias_publicas/settings.py	2026-05-26 08:20:14.000000000 -0300
+++ b/audiencias_publicas/settings.py	2026-05-26 09:05:31.127581620 -0300
@@ -207,8 +207,8 @@
 
 COMPRESS_ROOT_DIR = config('COMPRESS_ROOT_DIR', default='static')
 COMPRESS_ROOT = os.path.join(BASE_DIR, COMPRESS_ROOT_DIR)
-COMPRESS_ENABLED = config('COMPRESS_ENABLED', default=True)
-COMPRESS_OFFLINE = config('COMPRESS_OFFLINE', default=False)
+COMPRESS_ENABLED = config('COMPRESS_ENABLED', default=True, cast=bool)
+COMPRESS_OFFLINE = config('COMPRESS_OFFLINE', default=False, cast=bool)
 
 LIBSASS_SOURCEMAPS = 'DEBUG'
 
diff -ruN a/requirements.txt b/requirements.txt
--- a/requirements.txt	2026-05-25 16:15:24.000000000 -0300
+++ b/requirements.txt	2026-05-26 08:50:08.833561607 -0300
@@ -50,8 +50,8 @@
 pytest-asyncio==1.3.0
 pytest-cov==7.1.0
 coverage==7.14.0
-Faker==12.0.1
-mixer==7.2.2
+Faker==40.19.1
+model-bakery==1.23.4
 responses==0.26.1
 ipython==9.13.0
 ipdb==0.13.13
